TITLE: Upgrade Merchant Intelligence Copilot UI to Hackathon-Ready Product
+ Integrate AWS API Gateway (Prod Architecture)
+ Add GitHub Top-Right Link
+ Update README (Local + AWS)

PROJECT CONTEXT:
Repo: merchant-intelligence-copilot
Frontend: React + Vite + Tailwind
Backend:
- Local development: FastAPI server
- Production architecture: AWS API Gateway → Backend service (Lambda or container)

Existing backend endpoints:
- GET /health
- POST /generate-insights
  Payload: { csv_text: string, language?: "en" | "hi" | "mr" }

GOAL:
Make the UI interactive, attractive, and judge-ready, while clearly aligning
with AWS hackathon guidelines by supporting AWS API Gateway as the production
API layer. The app must work locally (FastAPI) and be configurable for API
Gateway using environment variables.

────────────────────────────────────────
OBJECTIVES (IMPLEMENT ALL)
────────────────────────────────────────

A) App Shell + Navigation
1) Build a global app layout with modern navigation:
   - Pages/routes:
     Dashboard
     Upload Data
     Copilot Chat
     Weekly Report
     Settings
     About
   - Sidebar on desktop + compact top navigation on mobile
2) Header branding:
   - App name: Merchant Intelligence Copilot
   - Team: Bharat Brain Wave
3) Add a subtle global info banner:
   - “AI‑assisted insights to support smarter business decisions.”
4) Ensure mobile responsiveness, spacing, and polished UI.

B) GitHub Link (Upper Right) — REQUIRED
1) Add a GitHub icon button in the upper-right of the header.
2) Read repo URL from VITE_GITHUB_URL.
3) Open link in new tab with rel="noreferrer noopener".
4) Tooltip: “View source on GitHub”.
5) Hide icon if VITE_GITHUB_URL is not set.
6) Icon must respect light/dark theme.

C) Theme Toggle (Light / Dark)
1) Add light/dark toggle near GitHub icon.
2) Persist theme in localStorage.
3) Use Tailwind dark-mode class strategy.

D) Upload Data Page (CSV Upload + Validation)
1) Create Upload Data page with:
   - Drag-and-drop CSV area
   - Browse file button
2) Validate:
   - .csv extension
   - Required columns:
     date, product_name, quantity_sold, price, revenue
3) Show:
   - File name and size
   - Preview first 5 rows
4) On “Analyze”:
   - Read CSV client-side
   - Call POST /generate-insights
5) Persist last insights and filename in localStorage.

E) Dashboard Upgrade (Judge-Friendly)
1) KPI cards:
   - Products analyzed
   - Alerts count
   - Average confidence
   - Top reorder item
2) Product selector:
   - Select product to inspect
3) Forecast chart:
   - Use Recharts
   - Plot 7-day forecast (yhat)
   - Confidence band if available
4) Insight cards/table:
   - Confidence badges:
     Green > 80
     Yellow 60–80
     Red < 60
   - Urgency badge (high / medium)
   - Alert chips (spike / drop / slow_moving)
5) “Why?” explainability:
   - Drawer/modal per product showing:
     - Demand reasoning
     - Reorder logic explanation
     - Confidence explanation
   - Language must be merchant-friendly.

F) Copilot Chat Page (API Gateway Ready)
1) Build chat UI:
   - Message list (user / assistant)
   - Input box
   - Language selector (EN / HI / MR)
   - Show confidence and disclaimer text
2) If /chat API is not available:
   - Show “Chat backend not connected yet.”
   - UI must not crash
3) Design chat calls to work via:
   - Local FastAPI OR
   - AWS API Gateway (same path)

G) Weekly Report Page
1) Weekly report layout:
   - Top 3 priorities
   - Expected impact
   - Risks / alerts
2) If backend endpoint is unavailable:
   - Generate client-side from latest insights
3) Include confidence and explanation notes.

H) About Page + Footer
1) About page must explain:
   - MSME problem
   - What the solution does
   - AI usage:
     Forecasting + Amazon Bedrock reasoning
   - Responsible AI:
     Confidence scoring + transparency
   - Hackathon context
2) Footer on all pages:
   - “© 2026 Bharat Brain Wave”
   - “Built for AWS AI for Bharat Hackathon”
   - Optional GitHub link

I) Environment Configuration (API Gateway Support)
Update frontend .env.example:

- VITE_API_BASE_URL=http://127.0.0.1:3000
- VITE_GITHUB_URL=https://github.com/<username>/<repo>

The app must work with:
- Local FastAPI URL
- Deployed AWS API Gateway URL
  (no code changes, env switch only)

J) README.md Update (MANDATORY)
Update README.md to include:

1) Features list reflecting the upgraded UI
2) Local Run Instructions (PowerShell):
   - Frontend:
     cd frontend
     npm install
     copy .env.example .env
     npm run dev
   - Backend:
     uvicorn app:app --reload --port 3000
3) API Architecture Explanation:
   - Local: Frontend → FastAPI
   - Production: Frontend → AWS API Gateway → Backend
4) Environment Variables:
   - VITE_API_BASE_URL
   - VITE_GITHUB_URL
5) Security Note:
   - npm audit warnings are dev-tool related
   - No secrets committed
6) 3-minute judge demo walkthrough

────────────────────────────────────────
CONSTRAINTS
────────────────────────────────────────
- Keep dependencies minimal
- No breaking changes
- Must work with npm run dev
- Must not hardcode API URLs
- Must not crash if chat/report APIs are missing
- Mobile responsive and polished

────────────────────────────────────────
ACCEPTANCE CHECKLIST
────────────────────────────────────────
1) npm run dev works
2) Header shows theme toggle + GitHub icon
3) CSV upload works with validation and preview
4) Dashboard shows KPIs, charts, badges, and Why drawer
5) About page and footer are present
6) README clearly explains Local vs AWS API Gateway usage

IMPLEMENT NOW:
Apply all frontend and README changes.
Run npm run dev and fix any issues.
Summarize modified files and how to verify end-to-end.
