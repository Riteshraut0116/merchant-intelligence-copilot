[build]
  command = "npm run build"
  publish = "dist"
  # If deploying from monorepo, Netlify will use this folder as base

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# For monorepo: Set base directory in Netlify dashboard to:
# merchant-intelligence-copilot-files/frontend
